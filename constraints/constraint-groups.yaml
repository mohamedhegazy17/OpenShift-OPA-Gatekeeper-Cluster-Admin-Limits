apiVersion: constraints.gatekeeper.sh/v1beta1
kind: LimitClusterAdminGroups
metadata:
  name: max-admin-groups
spec:
  match:
    kinds:
      - apiGroups: ["rbac.authorization.k8s.io"]
        kinds: ["ClusterRoleBinding"]
  parameters:
    maxAdminGroups: 3
